# Public App Readiness Findings Register

| ID | Severity | Area | Surface | Finding | Evidence | Recommended Direction | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PAR-F001 | Release blocker | Contrast and styling | Runbook export cluster, capability action buttons, deeplink actions, chat external-search buttons | Shared utility-token mismatches previously made multiple visible actions effectively invisible. The compatibility layer plus darker safe values now restore visible, compliant action styling in the audited light-theme surfaces. | Post-remediation computed-style checks on `http://localhost:8003` verified `Build Link` at `5.93:1`, capability `Test` at `5.02:1`, and disabled `Installed`/`Enable` states at `6.10:1`. Source review shows the compatibility layer in `src/web_app.py` now covers the missing `sky`, `slate`, `cyan`, `violet`, `rose`, and `amber` families used by the public UI. | Keep regression checks for shared action families and eventually reduce the need for runtime compatibility shims by aligning the delivered CSS with the live class vocabulary. | `closed` |
| PAR-F002 | High | Semantics and keyboard access | Header connection status trigger | The connection-details entry point is now a semantic button with an explicit accessible name, popup relationship, and expanded state. | Live DOM inspection verified a `button` trigger with `aria-controls="connection-details-popover"`, `aria-expanded`, and a dialog-backed popover titled `Connection Details`. | Preserve the semantic button pattern for future top-level status surfaces and regression-check any new header entry points. | `closed` |
| PAR-F003 | High | Dialog semantics | Chat modal, chat settings modal, settings modal, summary modal, connection popover | The shared overlay surfaces now expose dialog semantics, accessible title linkage, and correct modal behavior for modal versus popover contexts. | Live checks verified chat, chat settings, settings, and summary shells as `role="dialog"` with `aria-modal="true"`. The connection details popover now exposes `role="dialog"` with `aria-modal="false"` and `aria-labelledby="connection-details-title"`. | Keep using the shared dialog pattern for new overlays and include dialog-role regression checks in future public UI changes. | `closed` |
| PAR-F004 | High | Accessible names | Chat header actions and other icon-only controls | The audited chat icon-only controls and send action now expose explicit accessible names instead of relying on title text or icon glyphs alone. | Live chat checks verified labels for `Open chat settings`, `Clear chat history`, `Close chat`, suggested-query disclosure controls, the chat input (`Chat message input`), and the send action (`Send chat message`). | Continue requiring explicit `aria-label` values for icon-only actions and reject title-only naming patterns in future UI changes. | `closed` |
| PAR-F005 | High | Expandable controls | Report/session year-month-day-session hierarchy | The generated-report hierarchy now uses semantic buttons with exposed expanded state for the audited day and session rows. | Live snapshots of the Generated Reports surface show day and session rows rendered as buttons with `aria-expanded` state instead of generic clickable containers. | Maintain the button-plus-expanded-state pattern for any additional hierarchy levels or future artifact browsers. | `closed` |
| PAR-F006 | High | Navigation semantics | Workspace tabs, workflow tabs, summary modal tabs | The tab-like control groups now expose semantic tab relationships instead of relying on visual styling alone. | Live validation verified `Workspace views` and `Runbook personas` as `tablist` surfaces. A post-load summary-modal check verified a semantic `tablist`, tab buttons, and a `tabpanel` for the active summary view. | Keep using the shared tab model for any new subview selectors and regression-check async-loaded tab surfaces after content hydration. | `closed` |
| PAR-F007 | Medium | Contrast and readability | KPI cards and low-emphasis metrics | The dedicated KPI/helper-text contrast pass is complete and the audited mission-card metrics now clear practical WCAG 2.2 AA expectations in both light and dark themes. | Live rechecks measured light-theme values at `8.88:1`, `8.01:1`, `7.29:1`, `14.44:1`, and `8.19:1`, and dark-theme values at `5.73:1`, `5.74:1`, `6.38:1`, `6.29:1`, and `5.93:1`. | Preserve the theme-aware KPI-card and muted-text treatment, and rerun contrast checks whenever shared card colors or helper-text classes change. | `closed` |
| PAR-F008 | Medium | Public-facing language | Capability workspace, chat evidence labels, status metadata | The audited public UI now translates internal taxonomy into operator-facing language and keeps capability health copy current on load instead of surfacing stale persisted terminology. | Live capability snapshots now show `Capability Management`, `Local Artifact Search`, `Indexed Artifact Search`, `Create report packages for reports and presentations.`, `Report package generation is ready.`, and a human-readable latest-package label. API validation at `POST /api/capabilities/exports/build` returned `Report package generated.` and created `dt4sms_report_package_20260419_101515_admin_admin_discovery_package.zip`. | Keep capability titles/descriptions operator-facing, keep health refreshed when capability state is loaded, and avoid surfacing raw internal tokens without a visible translation layer. | `closed` |
| PAR-F009 | Medium | Frontend delivery risk | Entire public app shell | The public UI no longer ships through in-browser Babel or CDN-hosted runtime assets. A build-time pipeline now transpiles the legacy inline JSX into checked-in local static assets and vendors pinned local copies of React, ReactDOM, Tailwind `2.2.19`, and Font Awesome for runtime delivery. | `src/static/index.html` now references only `/static/...` assets, `src/static/app.js` is generated from the legacy inline template by `tools/build_frontend.mjs`, and live reload validation at `http://localhost:8003` no longer surfaces the previous Babel warning path. The release path now includes `npm run build:frontend` alongside `ruff`, strict `compileall`, and `unittest`. | Keep the frontend build step in the release path and remove the legacy inline fallback only if a later cleanup slice is worth the churn. | `closed` |