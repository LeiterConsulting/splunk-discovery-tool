# Chat Context Integrity Decision Log

| ID | Date | Decision | Rationale | Consequence |
| --- | --- | --- | --- | --- |
| D-001 | 2026-04-20 | Govern the reopened chat reliability issue under a dedicated `chat-context-integrity` `exec-ctrl` initiative | The issue crossed backend response finalization, persisted chat state, summary UI launch behavior, and validation evidence | The fix is tracked as a focused reliability slice instead of reopening unrelated prior initiatives |
| D-002 | 2026-04-20 | Normalize non-agentic conversation history into compact user/assistant turns | Report-backed, deterministic, and direct-answer routes were leaving stale server-format history authoritative, which let unrelated context bleed into the next request | The frontend now receives safe follow-up history after non-agentic routes, while the deep agentic loop retains its richer server-managed state |
| D-003 | 2026-04-20 | Launch summary investigations as fresh chat sessions and auto-send the selected prompt | Clearing only the visible input was insufficient because stale `serverConversationHistory` and `chat_session_id` memory both influenced the next turn | Summary `Investigate`, `Investigate in Chat`, `Build Validation Query`, and summary `Ask AI` now start isolated chat threads |
| D-004 | 2026-04-20 | Allow report-backed answers to surface optional RAG evidence | Strategic report routes previously returned before the optional RAG path ran, so indexed context could not influence those answers | Report-backed turns can now return `capability_usage` and an indexed-context addendum when the optional capability is enabled |
| D-005 | 2026-04-20 | Parse structured summary risk prompts and prefer focused risk routing over generic strategic keyword matching | Live browser validation showed a clicked platform-health risk was being misclassified as a generic use-case request because the impact text contained “use case” language | Summary risk investigations now return focused risk answers and risk-matched follow-on actions tied to the clicked report item |