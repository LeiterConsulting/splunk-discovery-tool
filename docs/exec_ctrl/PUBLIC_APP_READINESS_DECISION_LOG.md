# Public App Readiness Decision Log

| ID | Date | Decision | Rationale | Consequence |
| --- | --- | --- | --- | --- |
| D-001 | 2026-04-20 | Govern the public-readiness audit under its own `exec-ctrl` initiative | The work spans semantics, live UI behavior, contrast, public-facing language, and release-gate reporting across the main web surface | The effort now has its own control record, audit log, and decision log instead of being buried in chat context or attached to an unrelated initiative |
| D-002 | 2026-04-20 | Expand contrast review to explicitly include boxes, cards, badges, disclosures, and expandable surfaces | Public-facing contrast failures often occur in status panels, disclosure headers, and action clusters rather than plain body text alone | The findings register and audit log now treat invisible or near-invisible boxes and expandable controls as first-class defects |
| D-003 | 2026-04-20 | Treat transparent action backgrounds caused by unsupported utility tokens as release blockers | Live evidence showed visible primary actions rendering at `1.00:1` to `1.05:1` contrast because the intended background utilities were not applied | Remediation must address the shared styling root cause, not just recolor a few isolated buttons |
| D-004 | 2026-04-20 | Use live browser evidence plus source review as the audit standard | Source alone would not have exposed the transparent-button failures, while browser evidence alone would not have shown the Tailwind/version mismatch | Findings are documented with both live evidence and code-level root-cause context |
| D-005 | 2026-04-20 | Close `PAR-F007` after a dedicated light/dark KPI contrast pass | Live mission-card rechecks moved the previously weak helper and delta text above practical WCAG 2.2 AA thresholds in both themes | KPI/helper-text contrast no longer blocks initiative closeout and should be regression-checked only when card or muted-text styling changes |
| D-006 | 2026-04-20 | Close `PAR-F008` after translating capability terminology and refreshing health copy on load | The live capability workspace now uses operator-facing labels, current health text, and report-package naming instead of leaking raw internal taxonomy or stale persisted wording | Public-facing language cleanup is complete for the audited surfaces and future capability additions should follow the same formatter/definition pattern |
| D-007 | 2026-04-20 | Defer `PAR-F009` as a post-initiative architectural follow-up rather than a UX-closeout blocker | CDN-hosted frontend assets and in-browser Babel remain a real production-hardening risk, but the public UX/control-integrity objectives of this initiative are complete | The initiative can close with one explicit residual risk, while frontend delivery hardening remains a separate tracked follow-up before claiming a fully hardened production delivery model |
| D-008 | 2026-04-20 | Add a lightweight repo-level lint and compile gate instead of reopening public-readiness scope for a full frontend delivery migration | The next latent issues were real but narrow: stale imports, an actual Python local-scope bug, and embedded-frontend string escapes that unit tests alone would not reliably catch | The repo now carries `ruff.toml`, `requirements-dev.txt`, and a strict `compileall` warning gate as release-path guardrails, while the Babel/CDN delivery model remains explicitly deferred under `PAR-F009` |
| D-009 | 2026-04-20 | Resolve `PAR-F009` under a dedicated frontend-delivery hardening initiative instead of reopening the closed public-readiness implementation slice directly | The remaining risk was architectural and cross-cutting, so it needed its own bounded control pack and validation evidence rather than ad hoc edits inside a closed UX audit | Public readiness can stay closed while the residual delivery-model risk is handled through a dedicated follow-up initiative |
| D-010 | 2026-04-20 | Close `PAR-F009` after switching the runtime shell to checked-in local static assets and a build-time JSX pipeline | The app now serves `src/static/index.html` with local React, ReactDOM, Tailwind, Font Awesome, and transpiled app code, eliminating the previous CDN/Babel production path | The public runtime shell no longer carries the previously deferred medium-severity frontend delivery risk, though the legacy inline template remains as a fallback/build source |