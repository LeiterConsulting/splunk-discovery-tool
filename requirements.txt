# Discovery Tool for Splunk MCP Server (DT4SMS)
# Core dependencies - only what's actually used by the application

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Security and encryption
cryptography>=41.0.0

# LLM integration
openai>=1.0.0

# Configuration and data handling
pyyaml>=6.0

# HTTP clients
httpx>=0.24.0
aiohttp>=3.9.0
requests>=2.31.0  # For custom LLM endpoints (better Windows/vLLM compatibility)
